overwrite: true
schema: http://localhost:4000
# documents: ./src/**/*.graphql
generates:
  ./src/@types/graphql.d.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-urql
