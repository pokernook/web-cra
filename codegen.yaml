schema: ${GRAPHQL_SCHEMA_PATH}
overwrite: true
# documents: ./src/**/*.graphql
generates:
  ./src/@types/graphql.d.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-urql
hooks:
  afterAllFileWrite:
    - prettier --write
    - eslint --fix
