schema: ${GRAPHQL_SCHEMA_PATH}
overwrite: true
documents: src/**/*.graphql

generates:
  src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-urql

hooks:
  afterAllFileWrite:
    - eslint --fix
