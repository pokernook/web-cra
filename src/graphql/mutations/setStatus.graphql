mutation setStatus($emoji: String, $message: String) {
  userSetStatus(emoji: $emoji, message: $message) {
    createdAt
    emoji
    id
    message
    updatedAt
    # TODO: Should the status mutation really be returning user info?
    user {
      ...userFields
    }
  }
}
